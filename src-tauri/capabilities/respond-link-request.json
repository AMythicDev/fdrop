{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "respond-link-request",
  "description": "Capability for confirm link request window",
  "windows": ["respond-link-request-*"],
  "permissions": [
    "core:event:allow-emit-to",
    "core:window:allow-close"
  ]
}

