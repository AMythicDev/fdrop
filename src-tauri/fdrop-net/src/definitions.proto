syntax = "proto3";

package fdrop_net.definitons;

message Link {
    optional bool request = 1;
    optional LinkResponse response = 2;
}

enum LinkResponse {
    REJECTED = 0;
    ACCEPTED = 1;
    OTHER = 2;
}
